# Level 0 — foundation only. Each later level adds its own requirements file.
pydantic-settings>=2.0
pyyaml>=6.0
neo4j>=5.20              # only needed if you persist the graph (make docker-neo4j)
google-genai>=0.8        # only needed if LLM_PROVIDER=gemini

# Light document readers — born-digital PDF/Word/Excel/PowerPoint text
# extraction, no AI, no heavy ML (a few MB total). Docling (requirements-pdf.txt)
# is a much heavier fallback only for scanned pages and legacy .doc/.xls.
pypdf>=4.0
python-docx>=1.1
python-pptx>=0.6.23
openpyxl>=3.1
