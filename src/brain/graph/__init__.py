"""Level 2 — build the connected, asset-centric knowledge graph in Neo4j.

  validate.py : which (from -> relationship -> to) shapes the ontology allows
  resolve.py  : canonicalise equipment tags so duplicates merge into one node
  loader.py   : load staged facts -> nodes + clean relationships + chunks, with stats
"""
